let lista_tarefas = [];

function adicionar_tarefa(tarefa) {
    lista_tarefas.push(tarefa);
}

function remover_tarefa(tarefa) {
//*Obs: nao foi especificado que eu deveria programar essa parte então só fiz essa marcação, me corrija se eu estiver errado
//também não era possível alterar tarefas.js porque ele está em outro branch, então assumi que deveria criar tarefas.js novamente porém, no branch master
}